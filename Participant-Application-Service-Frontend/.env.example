# Where participant-application-service is running (apps/participant-application-service, PORT=3000).
# The browser never calls this directly: next.config.ts forwards /api/* to it.
BACKEND_URL=http://localhost:3000
